{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">  
        <div class="row justify-content-center"> 
            <div class="col-md-8">  
                <h1>Créer un Nouveau Post</h1>  

                <div class="form-error alert alert-danger" role="alert"></div> 

                {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}

                    <div class="mb-3"> 
                        {{ form_row(form.content, {'label': 'Contenu du Post', 'attr': {'class': 'form-control'}}) }}  
                    </div>
                    <div class="mb-3">
                        {{ form_row(form.image, {'label': 'Image du Post', 'attr': {'class': 'form-control'}}) }}
                    </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}